load guy, guyPalette from "littleGuy.png"

include "src/main/GrainLib/DMA.grain"
include "src/main/GrainLib/Hardware.grain"

func main(){
    setScreenDisabledAndBrightness(true, 15)

    dmaToVRam(guy, bankof(guy), sizeof(guy))
    dmaToCGRam(guyPalette, bankof(guyPalette), sizeof(guyPalette))
    executeDMA()

    enableInputAndNMI()
    setScreenDisabledAndBrightness(false, 15)

    while true do frame()
}

func VBlank(){

}