someGrid: word[32][32]
func resetGrid(){
    for y: word = 0; y < 32; y = y + 1 do
        for x: word = 0; x < 32; x = x + 1 do
            someGrid[y][x] = 0
}

include "src/main/GrainLib/DMA.grain"
include "src/main/GrainLib/Hardware.grain"
include "src/main/GrainLib/Backgrounds.grain"


class ArrayStorage{
    first2DArray: word[4][4]
    second2DArray: word[8][8]
    pointerArray: word ptr[8]
}
storage: ArrayStorage

func accessSomeData(){
    index: word = 4

    dataPtr: word ptr = (storage.first2DArray[4][storage.second2DArray[2][7]])@
    indirectValue: word = @(storage.pointerArray[index])
}


func main(){
    accessSomeData()
    resetGrid()

    asm{
        "ldx #1"
        "Top:"
        "cpx #1"
        //"beq Top"
    }
    something: word = 50 somethingElse: word = 7
    value: word = something %_ somethingElse
    asm{
        "tax"
    }

    while true do {}
}

func VBlank(){

}
